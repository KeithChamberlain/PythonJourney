import numpy as np # for mean, average nanmean, median, percentile, var & std (with ddof=1 or 0: Bessels correction)
from scipy import stats # for mode & more
import math # for factorial (or create own). 
